import { pageName } from 'coffee-shop/helpers/page-name';
import { module, test } from 'qunit';

module('Unit | Helper | page name');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = pageName([42]);
  assert.ok(result);
});
